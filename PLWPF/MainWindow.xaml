<Window x:Class="PLWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PLWPF" xmlns:be="clr-namespace:BE;assembly=BE" d:DataContext="{d:DesignInstance Type=be:Category}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <DataTemplate x:Key="ContentTemplate" DataType="local:ProductsList">
            <Grid>
                <TextBlock Text="ggg"/>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <ComboBox x:Name="comboBox" Margin="590,118,24,283" />
        <Image HorizontalAlignment="Left" Source="https://images.app.goo.gl/3Qhn9Dk6VUG7Y4hb6" Height="100" Margin="294,62,0,0" VerticalAlignment="Top" Width="100"/>
        <TabControl x:Name="TabControl1" Margin="10">            
            <TabControl.ItemContainerStyle>
                <Style TargetType="TabItem">
                    <Setter.Header>
                        
                    </Setter.Header>
                    <Setter Property="Header" Value="{Binding Name}"/>
                    <Setter Property="" Value="{Binding ContentTemplate}"/>
                </Style>
            </TabControl.ItemContainerStyle>
        </TabControl>

    </Grid>
</Window>




